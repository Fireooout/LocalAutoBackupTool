# 本地文件自动备份及管理工具 - 开发实行计划

## 1. 目标与基线
- 目标：在现有 `Local Auto Backup Tool v1.5.0.py` 单窗口工具基础上，升级为“多配置集成管理面板 + 配置独立窗口 + 全局设置 + 托盘协同”的现代化桌面应用。
- 当前基线能力（已具备）：多源备份、定时/手动备份、后缀策略（时间戳/序号/自定义）、备份管理（刷新/删除/还原/重命名）、快捷键、托盘、导入导出、隐藏文件跳过、中英文。
- 已识别差距：缺少“多配置卡片化管理面板”、缺少“每配置独立图标与启动按钮”、缺少“按配置统计空间显示”、缺少“全局关闭行为切换”等。

> 说明：仓库中当前未发现“开发思路.txt”文件（需求原文提到“发思路.txt”），本计划基于问题描述与现有代码实现整理。

## 2. 目标架构（建议）
采用“单进程多配置 + 分层模块化”：

1. **UI 层**
   - 主管理面板（卡片列表）
   - 配置管理窗口（每个配置可独立打开，支持多开）
   - 全局设置窗口
2. **应用服务层**
   - 备份调度服务（每配置独立定时器/线程状态）
   - 备份执行服务（复用现有备份与还原核心逻辑）
   - 应用启动服务（关联程序检测与启动）
   - 托盘协同服务（状态驱动图标与菜单）
3. **数据层**
   - 配置仓储（建议从单一 INI 迁移到结构化 JSON，同时保留导入旧 INI）
   - 图标与资源路径解析
   - 备份索引（每配置独立记录，替代单 `.source_path` 的耦合）

## 3. 数据模型设计（核心）
### 3.1 Profile（配置项）
- `id`（UUID）
- `name`（用户可编辑）
- `icon_path`（默认 `resources/default.ico`，若有关联程序则优先其图标）
- `enabled`（自动备份启停）
- `source_paths`（文件/目录数组）
- `dest_dir`
- `app_path`（可选，exe）
- `interval_minutes`（默认 10）
- `max_backups`（默认 5）
- `skip_hidden`（默认 false）
- `suffix_type`（timestamp/number/custom）
- `custom_suffix`
- `number_mode`（auto/manual）
- `start_number`、`counter`
- `duplicate_handling`（overwrite/rename/skip）

### 3.2 GlobalSettings（全局设置）
- `backup_hotkey` + `backup_hotkey_enabled`
- `restore_hotkey` + `restore_hotkey_enabled`
- `close_behavior`（`minimize_to_tray` 默认 / `exit`）
- `language`
- `theme`（预留）

## 4. 界面与交互落地方案
### 4.1 主管理面板（新）
- 卡片项：图标 + 名称 + 空间占用（自适应单位、保留 2 位小数）。
- 卡片左上：自动备份开关；右上：启动关联应用按钮。
- 点击卡片：打开该配置的管理窗口（允许并行多窗口）。
- 顶部菜单：设置（进入全局设置、导入导出、批量操作）。

### 4.2 配置管理窗口（按配置）
- **备份设置标签页**：源路径管理、备份目录、关联程序、间隔与最大数、隐藏文件、后缀策略、应用设置/恢复默认、开始自动备份/手动备份。
- **备份管理标签页**：备份列表、刷新、删除、还原、打开位置、重命名。

### 4.3 全局设置窗口
- 快捷键启用与按键设置（备份快捷键默认 `ctrl+F1`、还原快捷键默认 `ctrl+F2`，两者默认不启用）
- 重名文件处理策略（覆盖模式增加强提醒）
- 导入/导出设置
- 关闭按钮行为切换（默认最小化托盘）

### 4.4 托盘行为
- 左键单击：显示主面板
- 右键菜单：显示窗口 / 设置 / 中止所有自动备份 / 退出
- 图标状态：主程序与任务栏默认 `resources/folder-sync.ico`，任一配置启动自动备份后切换 `resources/folder-sync-start.ico`；配置卡片默认图标使用 `resources/default.ico`（若设置关联程序则优先程序图标）

## 5. 现代化 GUI 设计优化建议
1. **卡片式信息架构**：用统一卡片容器承载配置状态，减少页面跳转成本。  
2. **视觉层级统一**：主标题、卡片标题、元信息（空间占用）采用明确字号与灰度层级。  
3. **状态可视化**：启停、运行中、异常、最近备份时间采用颜色标签+图标双表达。  
4. **一致性组件库**：统一按钮尺寸、间距、圆角、悬停反馈；减少原生控件风格割裂。  
5. **响应式布局**：支持窗口缩放后自动换列（1/2/3列），避免固定分辨率拥挤。  
6. **非阻塞反馈**：长任务（扫描/统计/备份）用进度与状态栏，不阻塞主线程。  
7. **空状态与引导**：首次启动无配置时展示“新建配置向导”。  
8. **高 DPI 与主题兼容**：继续保留 DPI 适配，预留浅色/深色主题能力。  

## 6. 功能优化建议
1. **备份索引重构**：按配置独立维护元数据，避免多配置共享索引导致串扰。  
2. **空间统计缓存**：增量统计备份占用，避免每次全量扫描卡顿。  
3. **任务调度隔离**：每配置独立调度与错误隔离，单配置异常不影响全局。  
4. **配置迁移机制**：启动时自动迁移旧版 `backup_config.ini` 到新结构。  
5. **还原安全增强**：还原前冲突预览、目标校验、回滚提示。  
6. **日志分级与诊断**：按配置记录日志并支持导出诊断包。  
7. **异常自愈**：源路径失效时自动标记并提示修复，而非仅删除。  
8. **性能优化**：大目录复制采用批处理与限频 UI 更新，降低界面卡顿。  
9. **启动按钮健壮性**：校验 exe 有效性、不可用时给出修复入口。  

## 7. 分阶段开发路线（建议）
### 阶段A：架构重组与兼容层
- 拆分单文件为模块：`ui/`, `services/`, `models/`, `storage/`, `resources/`
- 建立新配置模型与旧 INI 迁移器
- 保留现有备份核心逻辑作为兼容执行器

### 阶段B：主面板与多配置能力
- 实现配置卡片列表、增删改、图标加载、空间占用显示
- 实现每配置启停开关与关联程序启动按钮

### 阶段C：配置窗口与备份管理能力迁移
- 将现有“备份设置/备份管理”迁移为按配置实例化窗口
- 打通“应用设置、恢复默认、手动备份、自动备份”

### 阶段D：全局设置与托盘联动
- 完成全局快捷键、关闭行为、导入导出
- 托盘菜单四项能力与动态图标切换

### 阶段E：质量与发布
- 回归测试（多配置并发、异常路径、重名策略、托盘交互）
- 打包与资源校验（`resources/default.ico`、`resources/folder-sync.ico`、`resources/folder-sync-start.ico`）

## 8. 验收清单
- 可在主面板管理多个配置，卡片展示图标/名称/空间占用。
- 每配置可独立打开管理窗口并独立运行自动备份。
- 关联应用程序可被启动，且图标可回显到主面板。
- 全局设置与托盘行为满足需求描述。
- 图标状态可随自动备份运行状态切换。
- 旧版用户配置可迁移并继续使用。
